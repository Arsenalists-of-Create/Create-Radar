package org.valkyrienskies.mod.common;

import org.valkyrienskies.mod.common.ship.LoadedShip;
import org.valkyrienskies.core.api.ships.ShipWorld;
import net.minecraft.world.level.Level;
import net.minecraft.core.BlockPos;

public class VSGameUtilsKt {
    public static LoadedShip getShipManagingPos(net.minecraft.world.level.Level level, BlockPos pos) { return null; }
    public static LoadedShip getShipObjectManagingPos(net.minecraft.world.level.Level level, double x, double y, double z) { return null; }
    public static ShipWorld getShipObjectWorld(net.minecraft.world.level.Level level) { return null; }
    public static ShipWorld getAllShips(net.minecraft.world.level.Level level) { return null; }
}